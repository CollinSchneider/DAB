
  $("<%=j render(:partial => 'tech') %>")
  .appendTo($(".infinite-table"));


// Update pagination link
<% if @tech.last_page? %>
  $('.pagination').remove();
<% else %>
  $('.pagination')
    .html("<%=j link_to_next_page(@tech, 'Next Page', :remote => true) %>");
<% end %>
