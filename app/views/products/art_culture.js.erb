
  $("<%=j render(:partial => 'art_culture') %>")
  .appendTo($(".infinite-table"));

// Update pagination link
<% if @art_culture.current_page === @art_culture.total_pages %>
  $('.pagination').remove();
<% else %>
  $('.pagination')
    .html("<%=j link_to_next_page(@art_culture, 'Next Page', :remote => true) %>");
<% end %>
