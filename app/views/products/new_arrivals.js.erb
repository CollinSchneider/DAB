
  $("<%=j render(:partial => 'new_arrival') %>")
  .appendTo($(".infinite-table"));

// Update pagination link
<% if @new_arrivals.current_page === @new_arrivals.total_pages %>
  $('.pagination').remove();
<% else %>
  $('.pagination')
    .html("<%=j link_to_next_page(@new_arrivals, 'Next Page', :remote => true) %>");
<% end %>
