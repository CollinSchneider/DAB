var rendered;
function loadProducts(){
  rendered = $("<%=j render(:partial => 'new_arrival') %>")
};
function appendProducts(){
  rendered.appendTo($('.infinite-table'))
}
loadProducts()
setTimeout(appendProducts, 200);
// Update pagination link
<% if @new_arrivals.current_page === @new_arrivals.total_pages %>
  $('.pagination').remove();
<% else %>
  $('.pagination')
    .html("<%=j link_to_next_page(@new_arrivals, '', :remote => true) %>");
<% end %>
