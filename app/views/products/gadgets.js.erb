
  $("<%=j render(:partial => 'gadget') %>")
  .appendTo($(".infinite-table"));

// Update pagination link
<% if @gadgets.last_page? %>
  $('.pagination').remove();
<% else %>
  $('.pagination')
    .html("<%=j link_to_next_page(@gadgets, 'Next Page', :remote => true) %>");
<% end %>
