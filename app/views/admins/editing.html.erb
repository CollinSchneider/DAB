

    <%= render partial: 'nav' %>
    <main>
      <div class="container">
        <% if flash[:error] %>
          <div class="error-div">
            <% flash[:error].each do |error| %>
              <h5 class = "error-text"> <%= error %></h5>
            <% end %>
          </div>
        <% end %>
        <% if flash[:success] %>
          <div class="success-div">
            <h5 class = "success-text"> <%= flash[:success] %></h5>
          </div>
        <% end %>
        <div class="row">
          <div class="col s12">
            <p class='account-header' style='text-align:center'>Edit Site</p>
            <%= form_for Banner.new do |f| %>
              <p> Banner Description </p>
              <%= f.text_field :description, :placeholder => "Description...", :required => true %>
              <p> Banner Image </p>
              <%= f.file_field :image, :required => true %> </br>
              <p> Make Active? </p>
              <%= f.select :status, :required => true do %>
                <% ['Active', 'Non-Active'].each do |a| %>
                  <%= content_tag(:option, a, value: a,) %>
                <% end %>
              <% end %>
              <p>Linked Page</p>
              <%= f.select :link_to, :required => true do %>
                <% ['Best Sellers', 'New Arrivals', 'Featured'].each do |a| %>
                  <%= content_tag(:option, a, value: a,) %>
                <% end %>
              <% end %>
              <%= f.submit "Create New Banner" %>
            <% end %>

            <% @banners.each do |banner| %>
              <div id = "banner-div" style="border:1px solid black; width: 49%; display: inline-block; text-align: center">
                <%= image_tag banner.image.url(:medium), :class => "responsive-img" %>
                <div id = "banner-info">
                  <h6> Title: <%= banner.description %></h6>
                  <h6> Status: <%= banner.status %> </h6>
                  <h6> Linked Page: <%= banner.link_to %></h6>
                  <%= button_to "Delete #{banner.description} banner", banner_path(banner.id), method: :delete %>
                  <button type="button" class="edit-button">Edit Banner</button>
                </div>
                <div id="edit-banner-div" style="display:none">
                  <h3>Edit Banner Div....</h3>
                  <%= form_for banner do |f| %>
                    <p> Banner Description </p>
                    <%= f.text_field :description, :placeholder => "Description...", :required => true %>
                    <p> Banner Image </p>
                    <%= f.file_field :image, :required => true %> </br>
                    <p> Make Active? </p>
                    <%= f.select :status, :required => true do %>
                      <% ['Active', 'Non-Active'].each do |a| %>
                        <%= content_tag(:option, a, value: a,) %>
                      <% end %>
                    <% end %>
                    <p>Linked Page</p>
                    <%= f.select :link_to, :required => true do %>
                      <% ['Best Sellers', 'New Arrivals', 'Featured'].each do |a| %>
                        <%= content_tag(:option, a, value: a,) %>
                      <% end %>
                    <% end %>
                    <%= f.submit "Create New Banner" %>
                  <% end %>
                  <button type="button" class="cancel-button">Cancel</button>
                </div>
              </div>
            <% end %>


          </div>
        </div>
      </div>
    </main>
    <script>
      $('.edit-banner').click(function(){
        console.log('banner id: ' + $(this).attr('id'));
        // $('#edit-banner-div').css({
        //   display: 'inherit'
        // });
        // $('#banner-info').css({
        //   display: 'none'
        // })
      })
      $('.cancel').click(function(){
        console.log('banner id: ' + $(this).attr('id'));
        // $('#edit-banner-div').css({
        //   display: 'none'
        // });
        // $('#banner-info').css({
        //   display: 'inherit'
        // })
      })
    </script>

  </body>
</html>
